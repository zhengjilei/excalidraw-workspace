services:
  backend:
    build: ./backend
    volumes:
      - excalidraw-data:/app/data
    expose:
      - "8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  excalidraw-data:
